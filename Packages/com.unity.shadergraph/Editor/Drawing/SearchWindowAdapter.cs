namespace UnityEditor.ShaderGraph
{
    // com.unity.searcher is intentionally removed from this embedded package to
    // allow the project to resolve in the current network environment. The
    // original search window adapter is therefore disabled together with the
    // editor entry points that depended on it.
}
